<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:beans="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
    http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.tws.activemq"/>
    <context:property-placeholder location="classpath:activemq.properties"/>

    <!-- connection pool -->
    <bean id="activemqConnectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory">
        <property name="brokerURL" value="${activemq.broker.url}"/>
        <property name="alwaysSessionAsync" value="true"/>
    </bean>
    <bean id="jmsConnectionFactory" class="org.apache.activemq.pool.PooledConnectionFactory"  destroy-method="stop">
        <property name="connectionFactory" ref="activemqConnectionFactory"/>
        <property name="maxConnections" value="20"/>
        <property name="createConnectionOnStartup" value="${activemq.create.connection.startup}"/>
    </bean>

    <!-- topic -->
    <bean id="tickQuoteTopic" class="org.apache.activemq.command.ActiveMQTopic">
        <property name="physicalName" value="${activemq.tickQuote.topic}"/>
    </bean>

    <!-- message listener -->
    <!--<bean id="stdoutMessageListener" class="com.tws.activemq.StdoutMessageListener"/>-->
    <!--<bean id="cassandraMessageListener" class="com.tws.activemq.CassandraMessageListener"/>-->

    <!--<bean id="tickQuoteListenerContainer" class="org.springframework.jms.listener.DefaultMessageListenerContainer">-->
        <!--<property name="connectionFactory" ref="jmsConnectionFactory"/>-->
        <!--<property name="messageListener" ref="cassandraMessageListener"/>-->
        <!--<property name="destination" ref="tickQuoteTopic"/>-->
    <!--</bean>-->

    <!--message publisher-->
    <bean id="myJmsTemplate" class="org.springframework.jms.core.JmsTemplate">
        <property name="connectionFactory" ref="jmsConnectionFactory"/>
        <property name="defaultDestination" ref="tickQuoteTopic"/>
        <property name="pubSubDomain" value="true"/>
        <property name="deliveryPersistent" value="false"/>
    </bean>

</beans>